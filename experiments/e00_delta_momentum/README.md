# E00 — Figure 4, standard vs delta momentum (done, CPU)

`python -m experiments.e00_delta_momentum.run --out results/e00` (seconds, deterministic).
Committed outputs in `results/`: `results.json`, `robustness_grid.json`, trajectories, and
`figure4_reproduction.png`. Findings and constants: docs/EXPERIMENT_PLAN.md (E00) and
docs/DEVIATIONS.md (D-E00-*).
